# Built CLI (Unix binary has no extension)
/cmd/googlesqlengine-cli/googlesqlengine-cli

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.idea

# golangci-lint (task lint:install)
/bin/

# Local direnv overrides (optional; see .envrc)
.envrc.local

# Unpacked libduckdb shared library for `task test:duckdb-lib` (local path via DUCKDB_LIB_DIR)
.duckdb-lib/

# Dependency directories (remove the comment below to include it)
# vendor/
.googlesqlengine_history

# Local Go workspace (template: go.work.dev)
go.work
go.work.sum
